# UI refactor review and merged Server baseline

The reviewed preview uses the refactored overview, connections, memory and settings pages. It is an unsigned internal Windows preview. Readiness for code review does not establish complete S4 release/platform qualification.

## Compatibility identity

The `sqlite-1.1.1-v1` profile identifies backend source `1e90376a18e80437a74f2f55141d4301dc7f89c4`, with OpenAPI SHA-256 `915866dd26fd2bdce6f987a52fed7febaa859eaf506412765e7c847ea530a6ee`. The qualification wheel is `powercontext-1.1.1.dev56+g3501e4a6e.d20260923-py3-none-any.whl`, SHA-256 `163973110ce96e4332d0304796efa2f98d45e3efca3964e66b9c8ebcdc8f8658`. It was built from the pre-author-correction checkout `3501e4a6`; its backend source, OpenAPI, project configuration and lock exactly match the identified upstream source. Later CI wheels carry their own build identity and reports.

Existing profiles selecting `sqlite-63f918b7-v1` must explicitly choose the new profile and recheck. The new contract is not silently attributed to the old Server package. Selecting a profile is an explicit compatibility choice, not remote binary attestation.

## Reviewed behavior and validation

- Frontend lint, generated contract/icon checks, TypeScript, production build and 21 behavior tests passed. The new regression verifies that overview refresh displays the latest readiness report, rather than the activation snapshot.
- The native suite passed 42 tests across API errors, TLS, transport budgets, credential persistence/session boundaries, generation invalidation, exact scopes, immutable writes, unknown outcomes, diagnostics and IPC authorization.
- The CLI adapter permits the qualified 1.1.1 series as well as the prior 1.0.1 series. It still verifies the registered exact version, executable path and digest before fixed commands. Real isolated 1.1.1 CLI diagnostics passed; the first local run timed out under concurrent validation load and a sequential rerun passed without changing production deadlines.
- Real four-mode SQLite Server qualification passed against the identified wheel: anonymous loopback, static Bearer, HTTPS/CA/base path, and injected-provider/enforced access. The harness verified save/search/exact reads, changed identity, binding revocation, committed-response-loss recovery and 51-item scope pagination.
- Repository hooks passed except three existing Windows `url2pathname` deprecation warnings in `src/powercontext/cli/opencode.py`. No baseline warnings were suppressed and no unrelated CLI changes were included.

The UI review checked explicit connection activation versus selection, credential write-only inputs, exact scope context, note byte limits, no Enter submission, bounded FTS, exact citation reading, plaintext rendering, clipboard actions and discarded-context cleanup. Remote installed-package acceptance remains a required check for the submitted head; consult the PR checks and run artifacts for its actual outcome.

The [Server report](ui-review-20260923-server.json) and [CLI report](ui-review-20260923-cli.json) retain the exact qualification wheel and measured local environment.

## Remaining qualification

Clean standard-user Windows 11 with/without WebView2, publisher signing, actual IME/screen-reader/high-contrast and full keyboard qualification, notification cold activation, named Agent capture/recall, organizational owners and approved performance budgets remain incomplete. No release, merge, issue closure or complete S4 qualification is implied by making the PR ready for review.
